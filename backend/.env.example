# File-backed SQLite database used by the backend.
DATABASE_URL=./identity.sqlite
# Development-only signing key; replace in deployed environments.
JWT_SIGNING_SECRET=dev-secret-change-in-production
# Exact frontend origin permitted to call the backend API.
FRONTEND_ORIGIN=http://127.0.0.1:3000
